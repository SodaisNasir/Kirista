import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import CustomHeader from '../../components/CustomHeader'

const VeiwEvent = () => {
  return (
    <View>
    <CustomHeader/>
    </View>
  )
}

export default VeiwEvent

const styles = StyleSheet.create({})